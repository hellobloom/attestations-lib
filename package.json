{
  "name": "attestations-lib",
  "version": "0.1.7",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "description": "Shared types and logic between blooms dapp and whisper docker.",
  "repository": "https://github.com/hellobloom/attestations-lib",
  "author": "Eddie Hedges <eddie@bloom.co>",
  "license": "UNLICENSED",
  "lint-staged": {
    "src/**/*.{ts,tsx,json,css}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "@types/node": "^10.9.4",
    "lint-staged": "^7.2.0",
    "prettier": "^1.13.7",
    "tslint": "^5.11.0",
    "typescript": "^2.9.2"
  },
  "scripts": {
    "build": "./bin/build",
    "prepack": "yarn build",
    "prepare": "yarn build",
    "watch": "./node_modules/.bin/tsc --watch"
  },
  "dependencies": {
    "@types/lodash": "^4.14.116",
    "lodash": "^4.17.10",
    "ts-node": "^7.0.1",
    "tsconfig-paths": "^3.5.0",
    "web3-utils": "^1.0.0-beta.36"
  }
}
